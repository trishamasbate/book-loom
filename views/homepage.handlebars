<main class="container mt-5">
  <h1 class="mb-4 text-black">Library</h1>
  <div class="row">
    {{#each posts}}
      <div class="col-md-4">
        <div class="card custom-card mb-3">
          <div class="card-header bg-custom">
            <h2 class="card-title">
              <a href="/post/{{id}}" class="text-dark text-decoration-none">{{title}}</a>
            </h2>
          </div>
          <div class="card-body text-dark">
            <p>Book Owner: {{user.username}} Date: {{format_date createdAt}}</p>
          </div>
        </div>
      </div>
    {{/each}}
  </div>
</main>
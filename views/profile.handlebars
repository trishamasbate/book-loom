<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <link rel="stylesheet" href="/path/to/your/styles.css">
</head>
<body>
    <header>
        <h1>{{user.username}}'s Profile</h1>
    </header>
    <main>
        <section id="book-management">
            <h2>Manage Your Books</h2>
            <form id="add-book-form">
                <input type="text" id="book-title" placeholder="Book Title" required>
                <input type="text" id="book-author" placeholder="Book Author" required>
                <button type="submit">Add Book</button>
            </form>
            <ul id="book-list">
                {{#each user.books}}
                <li data-book-id="{{this.id}}">
                    <span class="book-title">{{this.title}}</span> by <span class="book-author">{{this.author}}</span>
                    <button class="edit-book">Edit</button>
                    <button class="delete-book">Delete</button>
                </li>
                {{/each}}
            </ul>
        </section>
        <section id="requests-management">
            <h2>Manage Requests</h2>
            <ul id="requests-list">
                {{#each user.requests}}
                <li data-request-id="{{this.id}}">
                    <span class="request-info">{{this.username}} wants to borrow "{{this.bookTitle}}"</span>
                    <button class="approve-request">Approve</button>
                    <button class="deny-request">Deny</button>
                </li>
                {{/each}}
            </ul>
        </section>
    </main>
    <script src="/path/to/your/scripts.js"></script>
</body>
</html>